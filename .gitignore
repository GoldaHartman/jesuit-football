# Original school documents — the app publishes the schedule, not the PDFs.
source/
*.pdf
*.docx
*.xlsx

# The roster is deliberately kept out of this repo and out of the app.
roster*

# Camera originals never get committed. Only the processed, EXIF-stripped
# JPEGs under docs/photos/ go in — see tools/prepare_photos.py.
*.HEIC
*.heic
*.MOV
*.mov

.DS_Store
__pycache__/
venv/

# API keys — never committed
.env

# Single-file share build — regenerate with tools/build_standalone.py
Jesuit-Football.html

# Capacitor / native builds
node_modules/
ios/App/Pods/
ios/App/App/public/
ios/App/build/
android/app/src/main/assets/public/
android/.gradle/
android/build/
android/app/build/
android/local.properties
.DS_Store

# Waiting on the workflow token scope — see README
.github-pending/
